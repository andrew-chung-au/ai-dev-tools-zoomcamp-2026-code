[project]
name = "chores-app"
version = "0.1.0"
description = "Household chores rotation tracker (Django)"
requires-python = ">=3.12"
dependencies = [
    "django>=6.1.1",
]

[tool.uv]
package = false
