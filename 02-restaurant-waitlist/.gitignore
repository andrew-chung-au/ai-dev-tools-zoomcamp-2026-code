# Ignore contents of _session-summaries, not the folder
_session-summaries/*
!_session-summaries/.gitkeep

# Frontend lockfile noise when using npm in a Bun-based project
frontend/package-lock.json

# Local SQLite database (backend/config.py DATABASE_URL default)
backend/*.db
*.db